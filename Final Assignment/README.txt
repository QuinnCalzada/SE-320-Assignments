Run the server first to confirm it is working. When you want to switch to the multi threaded server you need to reset the port since they use the same one.

Additonally, in my own testing you might need to run the client a few times if it gets stuck showing only [SERVER] outputs.